{
  "name": "bpt",
  "version": "0.0.1",
  "scripts": {
    "test": "mocha --compilers js:babel-core/register --check-leaks test/index.js",
    "cov": "istanbul cover _mocha -- --compilers js:babel-core/register lib/bpt.js test/index.js",
    "tc": "npm run test && npm run cov"
  },
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "babel-core": "^6.2.1",
    "babel-preset-es2015": "^6.3.13",
    "istanbul": "^0.4.1"
  }
}
